EXPLICACIÓN:
He creado lo siguiente
1. Pestaña "🤖 Modo Simulación" (Demo)
Esta pestaña está diseñada para mostrarte cómo funciona el algoritmo XGBoost sin necesidad de datos reales. Su principal función es la demostración y la prueba de la interfaz.

Cómo usarla:
Generar el Modelo:

Verás un botón que dice "Generar Modelo de Simulación".

Acción: Haz clic en este botón.

Resultado: El servidor de Streamlit generará 500 filas de datos sintéticos (inventados con la lógica de que un suelo más pobre necesita más Biochar) y entrenará el modelo XGBoost con ellos. Verás un mensaje verde de éxito.

Interactuar con los Parámetros:

Aparecerán dos deslizadores (sliders):

pH del Suelo: Mueve este control entre 4.0 (muy ácido) y 9.0 (muy alcalino).

Materia Orgánica (%): Mueve este control entre 1.0% y 5.0%. [ElegÍ el rango $1.0\%-5.0\%$ para que el prototipo refleje las condiciones más frecuentes de los suelos agrícolas y para prevenir errores de predicción por intentar extrapolar a condiciones extremas (como desiertos o turberas).]

Acción: Mueve ambos deslizadores lentamente.

Resultado: El valor en "Dosis Sugerida (Simulada)" cambiará inmediatamente.

Observación Clave: Nota cómo la dosis aumenta si el pH es bajo y la Materia Orgánica es baja. Esto refleja la hipótesis básica que el modelo sintético está aprendiendo.
NOTA Base Agronómica (Rango Típico)
El rango $1\%-5\%$ representa la mayoría de los suelos agrícolas de labranza media a alta en climas templados y tropicales, que son los que típicamente se buscan mejorar con biocarbón.Menos de $1\%$: Se considera un suelo muy pobre, típicamente arenoso o muy degradado. Aunque existen, no son el rango central de la agricultura productiva.Más de $5\%$: Estos son suelos con un contenido orgánico excepcionalmente alto, como los suelos de turba o Mollisoles (suelos oscuros y ricos). En estos casos, la necesidad de agregar enmiendas como el biocarbón para mejorar la fertilidad suele ser menor.Establecer un rango central y realista como $1.0\%-5.0\%$ garantiza que el modelo sintético se centre en el escenario más común de toma de decisiones en la agricultura.
Es fundamental entender los rangos de entrada para que el modelo sea realista.La razón por la que en el Modo Simulación establecimos el rango de la Materia Orgánica (MO) entre $1.0\%$ y $5.0\%$ tiene tres bases principales: ecológica, agronómica y de modelado.1. Base Agronómica (Rango Típico). El rango $1\%-5\%$ representa la mayoría de los suelos agrícolas de labranza media a alta en climas templados y tropicales, que son los que típicamente se buscan mejorar con biocarbón.Menos de $1\%$: Se considera un suelo muy pobre, típicamente arenoso o muy degradado. Aunque existen, no son el rango central de la agricultura productiva.Más de $5\%$: Estos son suelos con un contenido orgánico excepcionalmente alto, como los suelos de turba o Mollisoles (suelos oscuros y ricos). En estos casos, la necesidad de agregar enmiendas como el biocarbón para mejorar la fertilidad suele ser menor.Establecer un rango central y realista como $1.0\%-5.0\%$ garantiza que el modelo sintético se centre en el escenario más común de toma de decisiones en la agricultura.2. Base del Modelado (Evitar Extrapolación Extrema). En el aprendizaje automático, especialmente cuando se trabaja con datos sintéticos o limitados (como en la Pestaña 2 cuando subas pocos datos), es crucial evitar que el usuario ingrese valores que el modelo nunca ha visto o que se encuentran muy lejos del rango de entrenamiento.

Riesgo de Extrapolación: Si el modelo solo ha sido entrenado con datos entre $1\%$ y $5\%$, y un usuario ingresa $15\%$ (un valor de turbera), la predicción de la dosis de Biochar será una extrapolación y es probable que sea muy inexacta o incluso ridícula.Propósito del Slider: El deslizador restringe la entrada a un dominio razonable para proteger al modelo y al usuario de obtener resultados sin sentido.

Conexión con el Concepto de Biochar:
El biocarbón se utiliza principalmente en suelos que presentan algún tipo de limitación. Si un suelo tiene un $1.0\%$ de MO, es un suelo con baja capacidad de retención de agua y nutrientes, por lo que la adición de Biochar es muy relevante.Si un suelo tiene un $5.0\%$ de MO, sus propiedades físicas y químicas ya son muy buenas. Aunque el Biochar puede mejorar aspectos como la adsorción de metales pesados, la necesidad de agregar grandes cantidades para mejorar la fertilidad general disminuye.

2. Pestaña "📂 Carga tus Datos Reales" (Uso Científico)
Esta es la pestaña crucial que convierte la demo en una herramienta científica real para tu investigación. Aquí es donde le entregas los resultados de tus experimentos para que XGBoost aprenda tus patrones únicos.

Requisito: El Archivo CSV
Antes de usar esta pestaña, necesitas tener un archivo de datos. Crea una hoja de cálculo (Excel, Google Sheets, etc.) con los siguientes tres encabezados (columnas) y guarda el archivo en formato CSV (Valores Separados por Coma):
ph: El pH medido del suelo.

mo: El porcentaje de materia orgánica del suelo.

dosis_efectiva: La cantidad de Biochar (en Ton/Ha) que se usó en ese experimento y que resultó en el mejoramiento que buscas (p. ej., máxima fertilidad o máxima adsorción, según el objetivo de tu estudio).

Cómo usarla:
Subir el Archivo:

Haz clic en el botón "Sube tu archivo CSV".

Acción: Sube el archivo CSV que contiene tus datos reales de suelo.

Entrenar el Modelo:

Haz clic en el botón "Entrenar XGBoost con mis datos".

Acción: El servidor lee tus datos, los divide y entrena un nuevo modelo XGBoost único para tus condiciones.

Resultado: Verás un mensaje de éxito con un valor de Precisión (R2). Cuanto más cerca esté este número de 1.0, mejor ha aprendido el modelo tus datos.

Hacer Predicciones Reales:

Una vez entrenado, aparecerán dos cuadros de entrada: "Ingresa pH real" e "Ingresa MO real".

Acción: Introduce los valores actuales del suelo que deseas mejorar.

Resultado: Haz clic en "Calcular (Modelo Real)" y obtendrás la dosis de Biochar recomendada, basada enteramente en la experiencia histórica de tus propios datos.


import streamlit as st
import pandas as pd
import xgboost as xgb
import numpy as np

# Configuración
st.set_page_config(page_title="Calculadora Biochar", layout="wide")

st.title("🧪 Prototipo: IA para Dosis de Biochar")
st.markdown("""
Esta aplicación demuestra el uso del algoritmo **XGBoost** (usado en *Scientific Reports, 2025*) 
para predecir la dosis de Biochar.
""")

# Creamos pestañas
tab1, tab2 = st.tabs(["🤖 Modo Simulación", "📂 Carga tus Datos Reales"])

# --- PESTAÑA 1: SIMULACIÓN ---
with tab1:
    st.warning("⚠️ **AVISO:** Este modo utiliza datos sintéticos para demostrar la interfaz.")
    
    # Modelo para Simulación
    @st.cache_resource
    def entrenar_modelo_simulado():
        np.random.seed(42)
        n = 500
        ph = np.random.uniform(4, 9, n)
        mo = np.random.uniform(1, 5, n)
        dosis = (9 - ph) * 2 + (6 - mo) + np.random.normal(0, 0.2, n)
        df_sim = pd.DataFrame({'pH': ph, 'MO': mo, 'Dosis': dosis})
        model_sim = xgb.XGBRegressor(objective='reg:squarederror')
        model_sim.fit(df_sim[['pH', 'MO']], df_sim['Dosis'])
        return model_sim

    if st.button("Generar Modelo de Simulación"):
        model_sim = entrenar_modelo_simulado()
        st.session_state['model_sim'] = model_sim
        st.success("Modelo simulado entrenado exitosamente. Ahora puedes ajustar los parámetros.")

    if 'model_sim' in st.session_state:
        model_sim = st.session_state['model_sim']
        st.divider()
        st.subheader("Simulación en Tiempo Real")
        col1, col2 = st.columns(2)
        with col1:
            val_ph = st.slider("pH del Suelo", 4.0, 9.0, 6.5)
        with col2:
            val_mo = st.slider("Materia Orgánica (%)", 1.0, 5.0, 2.5)
            
        pred = model_sim.predict(pd.DataFrame([[val_ph, val_mo]], columns=['pH', 'MO']))[0]
        st.metric(label="Dosis Sugerida (Simulada)", value=f"{pred:.2f} Ton/Ha")

# --- PESTAÑA 2: CARGA DE DATOS REALES ---
with tab2:
    st.header("Entrena con tus datos de laboratorio")
    st.info("Sube un archivo CSV con columnas: 'ph', 'mo', 'dosis_efectiva'")
    
    uploaded_file = st.file_uploader("Sube tu archivo CSV", type=["csv"])
    
    if uploaded_file is not None:
        df_real = pd.read_csv(uploaded_file)
        
        if st.button("Entrenar XGBoost con mis datos"):
            try:
                # Asumimos que las columnas son 'ph', 'mo', 'dosis_efectiva'
                X = df_real[['ph', 'mo']]
                y = df_real['dosis_efectiva']
                
                model_real = xgb.XGBRegressor(n_estimators=100, learning_rate=0.1)
                model_real.fit(X, y)
                score = model_real.score(X, y)
                
                st.session_state['model_real'] = model_real
                st.success(f"¡Modelo entrenado con TUS datos! Precisión (R2): {score:.4f}")
                
            except KeyError:
                st.error("Error: Asegúrate de que tu archivo CSV contenga las columnas 'ph', 'mo', y 'dosis_efectiva'.")
            except Exception as e:
                st.error(f"Error desconocido durante el entrenamiento: {e}")
                
    if 'model_real' in st.session_state:
        st.divider()
        st.subheader("Calculadora basada en TUS datos")
        r_ph = st.number_input("Ingresa pH real", value=6.0)
        r_mo = st.number_input("Ingresa MO real", value=2.0)
        
        if st.button("Calcular (Modelo Real)"):
            pred_real = st.session_state['model_real'].predict(pd.DataFrame([[r_ph, r_mo]], columns=['ph', 'mo']))[0]

            st.success(f"Dosis calculada: {pred_real:.2f} Ton/Ha")

